import RouteLoadingScreen from "@/features/sparkid/components/RouteLoadingScreen"

export default function Loading() {
    return (
        <RouteLoadingScreen
            title="Laboratuvar açılıyor"
            message="Masa, parçalar ve bağlantı noktaları hazırlanıyor..."
        />
    )
}
